# In Arbeitsverzeichnis wechseln
git clone --recursive https://github.com/JacobDannecker/cantera_ma.git
# In cantera_ma wechseln

# Muessen auf dem System installiert sein
sudo apt install scons
sudo apt install libboost-dev
sudo apt install libhdf5-de
sudo apt install python3-venv
sudo apt install doxygen

# Virtuelle python Umgebung in cantera_ma erstellen 
python3 -m venv ./can_env

# Aktivieren
source ./can_env/bin/activate

# can_env und Unterverzeichnisse ignorieren
echo "can_env/*" >> .gitignore

pip3 install cython
pip3 install scons
pip3 install pytest
pip3 install numpy
pip3 install ruamel.yaml
pip3 install setuptools
pip3 install packaging
pip3 install wheel
pip3 install pytest
pip3 install jinja2
pip3 install scons
pip3 install pyqt5
pip3 install matplotlib
pip3 install scipy

# Kompilieren -j$(nproc) flag nutzt alle Prozessoren

scons build prefix=$VIRTUAL_ENV -j$(nproc)
scons install prefix=$VIRTUAL_ENV -j$(nproc)

# Danach zum kompilieren im Verzeichnis cantera_ma
scons install


# Noch nuetzlich: Alias in .bashrc

alias can_env="cd ~/Pfad_ins_Arbeitsverzeichnis; source ./can_env/bin/activate;"

# Um alias zu benutzen .bashrc neu laden oder terminalemulator neu starten
source ~/.bashrc




